name: Minuta v163 finance center safe release
run-name: Minuta v163 - ${{ inputs.phase }} - ${{ inputs.commit_sha }}

on:
  workflow_dispatch:
    inputs:
      phase:
        description: Production-safe phase for v163
        required: true
        default: test-v163
        type: choice
        options: [test-v163, validate-production-v163, apply-production-v163, observe-production-v163]
      commit_sha: { description: Exact 40-character main commit SHA, required: true, type: string }
      backup_run_id: { description: Fresh encrypted backup run ID, required: false, default: "", type: string }
      restore_run_id: { description: Linked offline restore run ID, required: false, default: "", type: string }
      test_run_id: { description: Successful isolated v163 test run ID, required: false, default: "", type: string }
      validation_run_id: { description: Successful read-only v163 validation run ID, required: false, default: "", type: string }
      apply_run_id: { description: Successful v163 apply run ID, required: false, default: "", type: string }
      confirmation: { description: Enter BACKUP_VERIFIED for production apply, required: false, default: "", type: string }

permissions: { contents: read, actions: read }
concurrency: { group: "${{ inputs.phase == 'test-v163' && 'minuta-test-database' || 'minuta-production-database' }}", cancel-in-progress: false }
defaults:
  run: { shell: bash }

jobs:
  preflight:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with: { ref: "${{ inputs.commit_sha }}", fetch-depth: 0, persist-credentials: false }
      - name: Pin exact current main commit and phase
        env: { SHA: "${{ inputs.commit_sha }}", PHASE: "${{ inputs.phase }}", CONFIRMATION: "${{ inputs.confirmation }}", GH_TOKEN: "${{ github.token }}" }
        run: |
          set -euo pipefail
          [[ "$SHA" =~ ^[0-9a-f]{40}$ ]]
          test "$GITHUB_REF" = refs/heads/main
          test "$GITHUB_SHA" = "$SHA"
          test "$(git rev-parse HEAD)" = "$SHA"
          test "$(gh api "repos/$GITHUB_REPOSITORY/git/ref/heads/main" --jq .object.sha)" = "$SHA"
          case "$PHASE" in
            test-v163|validate-production-v163|observe-production-v163) ;;
            apply-production-v163) test "$CONFIRMATION" = BACKUP_VERIFIED ;;
            *) exit 1 ;;
          esac
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with: { node-version: 22 }
      - run: node minuta-online-booking/tests/finance-center-v163-static-test.mjs
      - run: node --check minuta-online-booking/commerce-management.js
      - run: node --check minuta-online-booking/finance-center.js
      - run: node --check minuta-online-booking/provider.js

  test-v163:
    if: inputs.phase == 'test-v163'
    needs: preflight
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment: minuta-test
    env:
      MINUTA_TEST_DATABASE_URL: ${{ secrets.MINUTA_TEST_DATABASE_URL }}
      MINUTA_PRODUCTION_PROJECT_REF: ${{ vars.MINUTA_PRODUCTION_PROJECT_REF }}
      MINUTA_TEST_PROJECT_REF: ${{ vars.MINUTA_TEST_PROJECT_REF }}
      PGOPTIONS: -c statement_timeout=120000 -c lock_timeout=10000
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with: { ref: "${{ inputs.commit_sha }}", persist-credentials: false }
      - run: sudo rm -f /etc/apt/sources.list.d/google-chrome*.list /etc/apt/sources.list.d/google-chrome*.sources && sudo apt-get update && sudo apt-get install --yes postgresql-client
      - name: Apply twice, exercise atomic operations, rollback and reapply
        run: |
          set -euo pipefail
          test "$MINUTA_TEST_PROJECT_REF" != "$MINUTA_PRODUCTION_PROJECT_REF"
          db="${MINUTA_TEST_DATABASE_URL/:6543/:5432}"
          marker="$(psql "$db" -X -qAt -v ON_ERROR_STOP=1 --set=expected_ref="$MINUTA_TEST_PROJECT_REF" <<'SQL'
          select count(*) from minuta_migration_guard.target where project_ref=:'expected_ref' and allow_migrations is true;
          SQL
          )"
          test "$(tr -d '[:space:]' <<<"$marker")" = 1
          psql "$db" -X -q -v ON_ERROR_STOP=1 -f minuta-online-booking/supabase-migration-v163.sql
          psql "$db" -X -q -v ON_ERROR_STOP=1 -f minuta-online-booking/supabase-migration-v163.sql
          psql "$db" -X -q -v ON_ERROR_STOP=1 -f minuta-online-booking/tests/finance-center-v163-integration.sql
          psql "$db" -X -q -v ON_ERROR_STOP=1 -f minuta-online-booking/supabase-migration-v163-rollback.sql
          state="$(psql "$db" -X -qAt -v ON_ERROR_STOP=1 -c "select json_build_object('metadataDropped',to_regclass('public.financial_expense_metadata') is null,'recordRpcDropped',to_regprocedure('public.record_minuta_expense_v163(uuid,text,smallint,text,bigint,date,uuid,uuid)') is null,'v132Preserved',to_regclass('public.financial_expense_sources') is not null and to_regprocedure('public.pay_minuta_supplier_expense_v132(uuid,uuid,uuid,uuid)') is not null)")"
          jq -e '.metadataDropped and .recordRpcDropped and .v132Preserved' <<<"$state" >/dev/null
          psql "$db" -X -q -v ON_ERROR_STOP=1 -f minuta-online-booking/supabase-migration-v163.sql
          psql "$db" -X -q -v ON_ERROR_STOP=1 -f minuta-online-booking/tests/finance-center-v163-integration.sql
      - name: Create isolated test attestation
        env: { SHA: "${{ inputs.commit_sha }}" }
        run: |
          jq -n --arg sha "$SHA" --arg run "$GITHUB_RUN_ID" \
            --arg migration "$(sha256sum minuta-online-booking/supabase-migration-v163.sql|cut -d' ' -f1)" \
            --arg rollback "$(sha256sum minuta-online-booking/supabase-migration-v163-rollback.sql|cut -d' ' -f1)" \
            '{status:"success",phase:"test-v163",commitSha:$sha,runId:$run,isolatedDatabase:true,
              coverage:["expense","expense-replay","payload-conflict","daily-flow","categories-v1","timezone","specialist-denial","outsider-denial","append-only","reversal","reversal-replay","rollback","reapply"],
              migrationSha256:$migration,rollbackSha256:$rollback,productionWritten:false,businessRowsChanged:false}' > "$RUNNER_TEMP/v163-test.json"
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with: { name: "v163-test-${{ github.run_id }}", path: "${{ runner.temp }}/v163-test.json", if-no-files-found: error, retention-days: 35 }

  validate-production-v163:
    if: inputs.phase == 'validate-production-v163'
    needs: preflight
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment: minuta-production
    env:
      SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
      MINUTA_PRODUCTION_PROJECT_REF: ${{ vars.MINUTA_PRODUCTION_PROJECT_REF }}
      PGOPTIONS: -c default_transaction_read_only=on -c statement_timeout=60000 -c lock_timeout=5000
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with: { ref: "${{ inputs.commit_sha }}", persist-credentials: false }
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with: { node-version: 22 }
      - run: sudo rm -f /etc/apt/sources.list.d/google-chrome*.list /etc/apt/sources.list.d/google-chrome*.sources && sudo apt-get update && sudo apt-get install --yes postgresql-client
      - name: Validate production prerequisites and v163 state read-only
        env: { SHA: "${{ inputs.commit_sha }}" }
        run: |
          set -euo pipefail
          test "$MINUTA_PRODUCTION_PROJECT_REF" = cawexmmrqjvothcbgjxr
          [[ "$SUPABASE_DB_URL" == *"$MINUTA_PRODUCTION_PROJECT_REF"* ]]
          db="$(node minuta-online-booking/scripts/production-db-target-guard.mjs --session-url)"
          bash minuta-online-booking/scripts/production-db-schema-guard.sh "$db"
          state="$(psql "$db" -X -qAt -v ON_ERROR_STOP=1 <<'SQL'
          begin transaction isolation level repeatable read read only;
          with flags as(select
            to_regclass('public.financial_expense_sources') is not null
              and to_regclass('public.financial_transactions') is not null
              and to_regprocedure('public.pay_minuta_supplier_expense_v132(uuid,uuid,uuid,uuid)') is not null prerequisites,
            to_regclass('public.financial_expense_metadata') is not null metadata_table,
            to_regprocedure('public.record_minuta_expense_v163(uuid,text,smallint,text,bigint,date,uuid,uuid)') is not null record_rpc,
            to_regprocedure('public.reverse_minuta_expense_v163(uuid,uuid,uuid,text)') is not null reverse_rpc,
            to_regprocedure('public.get_minuta_finance_expenses_v163(uuid,date,date)') is not null read_rpc)
          select json_build_object('prerequisitesReady',prerequisites,'mode',case
            when prerequisites and not metadata_table and not record_rpc and not reverse_rpc and not read_rpc then 'ready'
            when prerequisites and metadata_table and record_rpc and reverse_rpc and read_rpc
              and has_function_privilege('authenticated','public.record_minuta_expense_v163(uuid,text,smallint,text,bigint,date,uuid,uuid)','EXECUTE')
              and not has_function_privilege('anon','public.record_minuta_expense_v163(uuid,text,smallint,text,bigint,date,uuid,uuid)','EXECUTE')
              then 'full' else 'partial' end) from flags;
          rollback;
          SQL
          )"
          state="$(grep '^{' <<<"$state"|tail -n 1)"
          jq -e '.prerequisitesReady and (.mode=="ready" or .mode=="full")' <<<"$state" >/dev/null
          jq -n --arg sha "$SHA" --argjson state "$state" '{status:"success",phase:"validate-production-v163",commitSha:$sha,readOnly:true,productionWritten:false,state:$state}' > "$RUNNER_TEMP/v163-validation.json"
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with: { name: "v163-validation-${{ github.run_id }}", path: "${{ runner.temp }}/v163-validation.json", if-no-files-found: error, retention-days: 35 }

  apply-production-v163:
    if: inputs.phase == 'apply-production-v163'
    needs: preflight
    runs-on: ubuntu-latest
    timeout-minutes: 25
    environment: minuta-production
    env:
      SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
      MINUTA_PRODUCTION_PROJECT_REF: ${{ vars.MINUTA_PRODUCTION_PROJECT_REF }}
      PGSSLMODE: require
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with: { ref: "${{ inputs.commit_sha }}", fetch-depth: 0, persist-credentials: false }
      - name: Verify isolated test, validation, encrypted backup and linked restore
        env: { SHA: "${{ inputs.commit_sha }}", TEST: "${{ inputs.test_run_id }}", VALIDATION: "${{ inputs.validation_run_id }}", BACKUP: "${{ inputs.backup_run_id }}", RESTORE: "${{ inputs.restore_run_id }}", GH_TOKEN: "${{ github.token }}" }
        run: |
          set -euo pipefail
          for id in "$TEST" "$VALIDATION" "$BACKUP" "$RESTORE"; do [[ "$id" =~ ^[0-9]+$ ]]; done
          check_run(){ run="$(gh api "repos/$GITHUB_REPOSITORY/actions/runs/$1")"; test "$(jq -r .conclusion <<<"$run")" = success; test "$(jq -r .head_sha <<<"$run")" = "$SHA"; test "$(jq -r .head_branch <<<"$run")" = main; test "$(jq -r .event <<<"$run")" = workflow_dispatch; test "$(jq -r .path <<<"$run")" = "$2"; }
          mkdir -p "$RUNNER_TEMP/v163-evidence"/{test,validation,backup,restore}
          check_run "$TEST" .github/workflows/minuta-v163-safe-release.yml
          gh run download "$TEST" -R "$GITHUB_REPOSITORY" -n "v163-test-$TEST" -D "$RUNNER_TEMP/v163-evidence/test"
          jq -e --arg sha "$SHA" '.status=="success" and .commitSha==$sha and .isolatedDatabase and (.productionWritten|not) and (.businessRowsChanged|not)' "$RUNNER_TEMP/v163-evidence/test/v163-test.json" >/dev/null
          check_run "$VALIDATION" .github/workflows/minuta-v163-safe-release.yml
          gh run download "$VALIDATION" -R "$GITHUB_REPOSITORY" -n "v163-validation-$VALIDATION" -D "$RUNNER_TEMP/v163-evidence/validation"
          jq -e --arg sha "$SHA" '.status=="success" and .commitSha==$sha and .readOnly and (.state.mode=="ready" or .state.mode=="full")' "$RUNNER_TEMP/v163-evidence/validation/v163-validation.json" >/dev/null
          check_run "$BACKUP" .github/workflows/minuta-supabase-backup.yml
          age=$(( $(date +%s)-$(date -d "$(jq -r .created_at <<<"$run")" +%s) )); test "$age" -ge 0 && test "$age" -le 7200
          artifact="$(gh api "repos/$GITHUB_REPOSITORY/actions/runs/$BACKUP/artifacts" --jq '.artifacts[]|select(.expired==false and .size_in_bytes>0)|.name')"; [[ "$artifact" == minuta-* ]]
          gh run download "$BACKUP" -R "$GITHUB_REPOSITORY" -n "$artifact" -D "$RUNNER_TEMP/v163-evidence/backup"
          shopt -s nullglob; encrypted=("$RUNNER_TEMP/v163-evidence/backup/"*.dump.gpg); journals=("$RUNNER_TEMP/v163-evidence/backup/"*.json)
          test "${#encrypted[@]}" = 1 && test "${#journals[@]}" = 1
          test "$(sha256sum "${encrypted[0]}"|cut -d' ' -f1)" = "$(jq -r .encryptedSha256 "${journals[0]}")"
          check_run "$RESTORE" .github/workflows/minuta-supabase-restore-drill.yml
          gh run download "$RESTORE" -R "$GITHUB_REPOSITORY" -n "minuta-ephemeral-restore-$RESTORE" -D "$RUNNER_TEMP/v163-evidence/restore"
          jq -e --arg backup "$BACKUP" --arg sha "$SHA" '.status=="success" and .networkMode=="none" and .ephemeralContainerDestroyed and (.productionWritten|not) and .sourceBackupRunId==$backup and .sourceBackupSha==$sha' "$RUNNER_TEMP/v163-evidence/restore/minuta-ephemeral-restore.json" >/dev/null
          test "$(gh api "repos/$GITHUB_REPOSITORY/git/ref/heads/main" --jq .object.sha)" = "$SHA"
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with: { node-version: 22 }
      - run: sudo rm -f /etc/apt/sources.list.d/google-chrome*.list /etc/apt/sources.list.d/google-chrome*.sources && sudo apt-get update && sudo apt-get install --yes postgresql-client
      - name: Apply v163 and verify default-empty protected schema
        env: { SHA: "${{ inputs.commit_sha }}", GH_TOKEN: "${{ github.token }}" }
        run: |
          set -euo pipefail
          db="$(node minuta-online-booking/scripts/production-db-target-guard.mjs --session-url)"
          bash minuta-online-booking/scripts/production-db-schema-guard.sh "$db"
          test "$(gh api "repos/$GITHUB_REPOSITORY/git/ref/heads/main" --jq .object.sha)" = "$SHA"
          psql "$db" -X -q -v ON_ERROR_STOP=1 -f minuta-online-booking/supabase-migration-v163.sql
          state="$(PGOPTIONS='-c default_transaction_read_only=on' psql "$db" -X -qAt -v ON_ERROR_STOP=1 -c "select json_build_object('ready',to_regclass('public.financial_expense_metadata') is not null and to_regprocedure('public.get_minuta_finance_expenses_v163(uuid,date,date)') is not null and has_function_privilege('authenticated','public.record_minuta_expense_v163(uuid,text,smallint,text,bigint,date,uuid,uuid)','EXECUTE') and not has_function_privilege('anon','public.record_minuta_expense_v163(uuid,text,smallint,text,bigint,date,uuid,uuid)','EXECUTE'),'metadataRows',(select count(*) from public.financial_expense_metadata))")"
          jq -e '.ready and .metadataRows==0' <<<"$state" >/dev/null
          jq -n --arg sha "$SHA" --argjson state "$state" '{status:"success",phase:"apply-production-v163",commitSha:$sha,state:$state,productionWritten:true,businessRowsChanged:false,secretsExposed:false}' > "$RUNNER_TEMP/v163-apply.json"
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with: { name: "v163-apply-${{ github.run_id }}", path: "${{ runner.temp }}/v163-apply.json", if-no-files-found: error, retention-days: 90 }

  observe-production-v163:
    if: inputs.phase == 'observe-production-v163'
    needs: preflight
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment: minuta-production
    env:
      SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
      MINUTA_PRODUCTION_PROJECT_REF: ${{ vars.MINUTA_PRODUCTION_PROJECT_REF }}
      PGOPTIONS: -c default_transaction_read_only=on -c statement_timeout=60000 -c lock_timeout=5000
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with: { ref: "${{ inputs.commit_sha }}", persist-credentials: false }
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with: { node-version: 22 }
      - run: sudo rm -f /etc/apt/sources.list.d/google-chrome*.list /etc/apt/sources.list.d/google-chrome*.sources && sudo apt-get update && sudo apt-get install --yes postgresql-client
      - name: Verify apply evidence and observe v163 read-only
        env: { SHA: "${{ inputs.commit_sha }}", APPLY: "${{ inputs.apply_run_id }}", GH_TOKEN: "${{ github.token }}" }
        run: |
          set -euo pipefail
          [[ "$APPLY" =~ ^[0-9]+$ ]]
          run="$(gh api "repos/$GITHUB_REPOSITORY/actions/runs/$APPLY")"
          test "$(jq -r .conclusion <<<"$run")" = success
          test "$(jq -r .head_sha <<<"$run")" = "$SHA"
          test "$(jq -r .path <<<"$run")" = .github/workflows/minuta-v163-safe-release.yml
          mkdir -p "$RUNNER_TEMP/v163-apply-evidence"
          gh run download "$APPLY" -R "$GITHUB_REPOSITORY" -n "v163-apply-$APPLY" -D "$RUNNER_TEMP/v163-apply-evidence"
          jq -e --arg sha "$SHA" '.status=="success" and .commitSha==$sha and .productionWritten and (.businessRowsChanged|not)' "$RUNNER_TEMP/v163-apply-evidence/v163-apply.json" >/dev/null
          db="$(node minuta-online-booking/scripts/production-db-target-guard.mjs --session-url)"
          bash minuta-online-booking/scripts/production-db-schema-guard.sh "$db"
          state="$(psql "$db" -X -qAt -v ON_ERROR_STOP=1 -c "select json_build_object('ready',to_regclass('public.financial_expense_metadata') is not null and to_regprocedure('public.get_minuta_finance_expenses_v163(uuid,date,date)') is not null,'metadataRows',(select count(*) from public.financial_expense_metadata))")"
          jq -e '.ready and .metadataRows>=0' <<<"$state" >/dev/null
          psql "$db" -X -q -v ON_ERROR_STOP=1 <<'SQL'
          begin transaction isolation level repeatable read read only;
          do $probe$
          declare target record; money jsonb;
          begin
            for target in
              select distinct on (membership.organization_id)
                membership.organization_id, membership.user_id
              from public.organization_memberships membership
              where membership.active and membership.role in ('owner','admin')
              order by membership.organization_id,
                case membership.role when 'owner' then 0 else 1 end,
                membership.user_id
            loop
              perform set_config('request.jwt.claim.sub',target.user_id::text,true);
              money:=public.get_minuta_finance_expenses_v163(target.organization_id,current_date-30,current_date);
              if money->>'organization_id'<>target.organization_id::text then
                raise exception 'v163_production_read_probe_context_mismatch';
              end if;
            end loop;
          end
          $probe$;
          rollback;
          SQL
          jq -n --arg sha "$SHA" --arg apply "$APPLY" --argjson state "$state" '{status:"success",phase:"observe-production-v163",commitSha:$sha,applyRunId:$apply,readOnly:true,productionWritten:false,state:$state}' > "$RUNNER_TEMP/v163-observe.json"
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with: { name: "v163-observe-${{ github.run_id }}", path: "${{ runner.temp }}/v163-observe.json", if-no-files-found: error, retention-days: 90 }
