# Повторная приёмка заимствованных решений

Статус: аудит начат по уточнению пользователя. Это не отчёт о завершённой
перепроверке и не доказательство превосходства над YCLIENTS.

## Охват и происхождение

Проверяются идеи, бизнес-правила, ограничения, интерфейс и сценарии, ранее
заимствованные у YCLIENTS: как опубликованные, так и находящиеся в разработке.
Отдельно проверяется корректность нашей реализации идеи: наш дефект нельзя
приписывать конкуренту. Сходство функций само по себе не доказывает заимствование.

Начать с P01–P18 дорожной карты, затем сверить связанные документы, историю
коммитов и доступные прежние исследования. Внеплановые находки добавлять отдельными
строками; неизвестное происхождение сохранять как незакрытый вопрос, не угадывать.
Для полноты нужен список просмотренных источников и оставшихся недоступных данных.
Нельзя объявлять «перепроверили всё» только после чтения 18 строк или запуска smoke.

## Разделённая очередь первого прохода

| Охват | Исполнитель | Статус повторной приёмки |
| --- | --- | --- |
| P01–P05: запись, журнал, ресурсы, группы/серии, ожидание | operations_gap | Назначено: код, тесты, источники и пробелы |
| P06–P09 и P18: CRM, удержание, филиалы/роли, уведомления, мобильность/офлайн | yclients_reference | Назначено: код, тесты, источники и UX |
| P10–P17: платежи, денежный журнал, зарплата, склад, материалы, продукты, лояльность, отчёты | finance_gap | Назначено: денежные инварианты, код/контракты и источники |
| Сведение реестра, границы доказательств, приоритеты и приёмка | Координатор | В работе |

Первый проход выполнялся read-only. Последующие подтверждённые дефекты исправляются
отдельными назначениями в изолированных worktrees с независимым review. Этот реестр
и дорожную карту изменяет только координатор. Текущие владельцы файлов и очереди
публикации проверяются заново, а не выводятся из исторической таблицы выше.

## Карточка каждого решения

Сохранять P-ID/дополнительный ID, пользовательскую потребность, источник и дату
либо «происхождение не подтверждено», принятую у нас модель, ограничения,
точные файлы/SHA, среду, воспроизводимый сценарий, фактический результат,
оставшиеся проверки и решение: оставить, улучшить, заменить или отдельно
согласовать отказ. Не удалять работающую функцию по одной гипотезе.

Статусы: инвентаризировано → проверена модель → проверен код → пройдены тесты
→ подтверждён сквозной сценарий → повторно принято. Статус «не применимо»
требует обоснования; исключение из обещанного объёма — решения пользователя.

## Критерии качества Minuta

| Область | Измеримый критерий приёмки |
| --- | --- |
| Повторы и конфликты | Потерянный ответ после commit и повтор одного запроса не создают второй визит, списание или выплату. За последнее место/ресурс проходят только допустимые операции; итог проверяется серверно |
| Деньги и остатки | Суммы сходятся до копейки, движения объясняются документами; частичный отказ не оставляет половину операции. Маркер paid/выдача права не выдаются за движение денег |
| Права и контекст | Две организации и разные роли не видят чужие данные. Поздний ответ после logout/смены организации/формы не меняет новый экран и не запускает чужие действия |
| Восстановление | Исключение запроса не оставляет вечную блокировку. Неоднозначный результат показан честно, исходные данные сохранены; повтор разрешён только безопасным способом |
| Понятность на телефоне | На ширинах 360/390 px видны полностью телефон, имя, услуга и заметки; ошибки и статусы объясняют следующий шаг. Основной сценарий проверяется клавиатурой и не зависит только от цвета |
| Скорость и сложность | На фиксированных устройстве/сети/данных записывать время и число действий до результата, ошибки пользователя и сохранность данных. Целевой предел задавать до изменения; ускорение не принимается ценой потери информации или защиты |
| Обновление и откат | Сбой загрузки новой версии оставляет рабочую прежнюю; после повторного входа, обновления и безопасного отключения данные/история сохранены. Проверка кэша не подменяет проверку реальной PWA и сервера |

Наличие теста не означает прохождение критерия: записывать запуск, SHA и границу
доказательства. Для конкурентных SQL-сценариев нужны независимые соединения;
подставные RPC и PGlite не доказывают корректность production-конкуренции.
Приёмка внешних каналов/денег проводится в разрешённой тестовой среде;
реальные сообщения и финансовые операции не выполняются без отдельной власти.

## Порядок исправлений

Сначала потеря/дублирование денег и записей, утечки и нарушения прав; затем
зависания и восстановление; затем удобство, скорость и полнота функций.
Безопасная независимая работа продолжается параллельно. Результат считается
лучше конкретного сравниваемого сценария только при сопоставимых измерениях;
нельзя обещать систему без каких-либо ошибок.

## Приёмка переноса после v445, 5 сентября 2026 года

Изменение P02 принято локально поверх `82953954bb52c6b97e4ce67d0b37a7098f080516`:
runtime `6da231e`, VM-regressions `cf584b84` + `6da231e`, независимые native tests
`62aafde` + `6c14e85`. В редакторе проверяются identity/epoch/org/session до действий
и после каждого await; отдельный request revision защищает загрузку времени.
Color/note helpers принимают необязательный guard и проверяют его до изменений
и после RPC, чтобы поздний ответ не сохранял старые данные в кэш нового аккаунта.
У остальных callers поведение defaults сохранено; их безопасность этим не доказана.

На интегрированном provider прошли VM переноса 48/48, VM отмены 50/50,
native переноса 35/35, native отмены 11/11 и free-slots scope 10/10.
Cancellation harness дополнен извлечением настоящего `bookingEditorRevision`,
поскольку общие close/reset/org hooks теперь используют оба epoch; assertions
не ослаблены. Native проверяет настоящие DOM handlers, color/note helpers,
persist и localStorage, но использует подставные RPC/refresh/Telegram и
navigation spies вместо полного renderer. Это не authenticated provider E2E.

Malformed/unknown series response не даёт ложного подтверждения переноса.
Отказ дополнительных сохранений или обновления журнала после подтверждённого
основного изменения показывается как частичный результат, а не как откат.
Изолированный реальный service worker прошёл четыре фазы обновления v445→v446,
включая отказ установки и offline SHA-256 ресурсов. CI, Pages и live health
проверяются отдельно после фиксации release SHA; этот раздел сам по себе не
подтверждает публикацию или выполнение серверного переноса.

## Проверенный срез v444, 5 сентября 2026 года

Этот раздел обновляет статус отдельных находок первого прохода. Он не означает
приёмку всего P01–P18. Происхождение конкретного кода от YCLIENTS по-прежнему
не доказано; ниже зафиксированы наши дефекты и наши проверки.

Опубликованный frontend: `2303d662f1a8f819b2177a526bbb8641902fe3bf`, cache v444.
Все пять branch CI и пять main CI прошли; [Pages 33985184940](https://github.com/aladushka9180-droid/anatomy-trainer/actions/runs/33985184940)
успешен. Read-only production health с ожидаемой версией 444 прошёл. SHA-256
`provider.js`, `app.js`, `free-slots-share.js`, `sw.js`, `provider.html`, `index.html`
совпали с байтами Git-коммита, а не с преобразованными Windows CRLF-копиями.
После этого среза текущий `main` и владельца следующего выпуска сверять заново.

| Находка | Решение и подтверждение на срезе | Что остаётся непроверенным или открытым |
| --- | --- | --- |
| P01, потерянный ответ публичной записи | Сохранение исходного request ID и immutable payload опубликовано в v442; восстановление DOM филиала B вместо primary A — в v443. VM 25/25 и полный public-app native 3/3 прошли. Реальный bundled SDK повторяет transport запросы; проверяются один ID, одинаковые параметры и одна запись mock ledger, а не искусственное число HTTP-запросов | Реальные конкурентные SQL-соединения и authenticated production lost-reply E2E не выполнялись |
| P02, отмена серии A после открытия B | В v444 опубликованы form/revision/org/session guards до и после RPC/helper refresh. Unknown/malformed response не закрывает форму и не объявляет отмену. Envelope сверяется с v79, UUID и occurrence уникальны, scope one относится к исходной записи. VM 50/50, native DOM с actual handlers 11/11 прошли; исходная версия воспроизводила stale-form ошибки | Native harness использует mock RPC и соседние контроллеры; это не полный authenticated provider E2E. Отмена оплаченной записи не доказывает денежный возврат |
| P02, перенос серии | Отдельный test-only `f03a9cc` воспроизводит stale effects после RPC/color/note/refresh: VM 2 PASS / 10 FAIL. Независимый native `62aafde` дал 1 PASS / 4 FAIL. Исправление назначено отдельной ветке | На срезе v444 runtime переноса не исправлен. Подмена финального openBookingSheet счётчиком доказывает stale navigation call, но не весь renderer |
| E-P01-SHARE, публикация свободных окон | В v443 добавлена инвалидация org/session/revision и пятисекундного подтверждения, suppression late output/fallback. Native 10/10 и прежний publication browser прошли; v444 повторно сохранил эти проверки | Начатое системное share/clipboard нельзя отозвать. Provider wiring проверяется извлечённым actual callback, не всей authenticated страницей |
| P10, денежные границы возврата | В v442 опубликованы integer-cent parsing, минимум 100 копеек и остаток 0 либо не менее 100, сохранение выбранного payment attempt. VM 49/49 и native 6/6 прошли | Эти проверки не закрывают неизвестный результат возврата и не являются реальными банковскими операциями |
| P10, lost-reply refund retry | `67590f6` и уточняющий `814179d` сохраняют честное воспроизведение: новый request ID при повторе может дать второй partial refund. После review: 5 PASS / 4 FAIL; отдельно разрешён законный явно новый возврат той же суммы | Synthetic provider counters не являются provider E2E; thrown invoke — defensive case, обычный bundled SDK возвращает error envelope. Browser reload recovery — отдельный будущий контракт |
| P10, backend recovery draft | Local-only sidecar `e095c8e` / disable `d877b33`: private receipts, own-actor lookup/list/explicit ACK, keyset за 50 записей, disable/reapply, PGlite 23/23. Prepare-only `b7c6889`: immutable private intent, exact replay/changed-payload refusal, min/cap/tail, PGlite 26/26 | Не миграции и не main runtime. Настоящий legacy prepare обходит enrolled creating intent по exact/new same-amount key — integration BLOCKED. Нет provider-body/dispatch/24h и browser wiring; legacy manager SELECT существующей refunds таблицы сохранён |
| P06/P09/P14, CRM v112–v114 | Test-only apply → reverse rollback → reapply завершён на `4e73ff98a2a44e857915356b18a265edb025cfe6`, run 33984549873. Реальная тестовая схема выявила отсутствующий UNIQUE bookings(id, organization_id); исправлена v113 и PGlite fixture, прежде скрывавшая проблему | Это отдельная анонимизированная testDB, default-off; не production migration/Edge deployment, не Storage HTTP E2E и не реальные отправки. Полную устаревшую frontend-ветку CRM в main не переносить |
| P18, доставка ресурсов PWA | Реальный isolated service worker: update v443→v444, failed-install recovery и offline SHA всех шести app/free-slots/provider/group/benefit/retention JS прошли, 4/4 фазы | Инертная HTML-оболочка не запускает app/provider bootstrap. Не проверялись установленная Android/iOS PWA и её authenticated offline workflow |

Свидетельство test-only CRM: [exercise 33984549873](https://github.com/aladushka9180-droid/anatomy-trainer/actions/runs/33984549873),
предшествующий [backup 33984056948](https://github.com/aladushka9180-droid/anatomy-trainer/actions/runs/33984056948)
закреплён на `bd7d46e863b4076ecfd238c05d9bd0c36bbc3a91`. Сохранены counts и fingerprints
24 исходных наборов колонок. Это не основание автоматически разрешать production gate.

Следующие приоритеты: закрыть runtime переноса серии; закрыть legacy refund bypass
и только затем проектировать исполнение/восстановление реального возврата;
пройти отдельные server concurrency, Storage и authenticated UI gates.
Новые локальные draft-тесты не повышают опубликованную готовность сами по себе.

## Первый независимый проход: исторические результаты и незакрытые проверки

Срез кода: frontend `bd697f0` и документальная ветка `6e89373`; CRM v113
изучался отдельно на `4b0c3b8`, v112/v114 — на `c0767a2`.
Это разные ветки, не единый production-релиз. После интеграции сверять SHA заново.
Историческое происхождение конкретного кода P01–P18 из YCLIENTS не доказано:
официальные ссылки дорожной карты подтверждают ориентир сравнения, не генеалогию.

| ID | Что установлено о нашей системе | Следующий обязательный сценарий |
| --- | --- | --- |
| P01 | Есть серверная идемпотентность; форма после unknown может сменить request ID при изменении контактов/времени | Commit + lost reply + редактирование: разрешить исходный результат без скрытого второго визита; 8 конкурентных соединений |
| P02 | Повторы серий имеют throw/stale-modal риски, отдельно воспроизведённые VM | Поздний ответ A не закрывает/не открывает форму B; все post-await действия проверяют контекст |
| P03 | v69 исключает пересечения ресурсов; выбор конкретного аппарата и отдельный буфер не доказаны | Два сотрудника/один ресурс: один победитель; отказ переноса сохраняет прежнее выделение |
| P04 | Недельные серии ограничены своим исполнителем; закрытые группы не имеют административного добавления участника | Ролевые сценарии и гонка за последнее место; one/following/all меняет только выбранные записи |
| P05 | v111 позволяет ручной статус booked без booking_id; это не подтверждённая конверсия | Серверная связанная конверсия: два администратора и повтор дают один визит и одно закрытие |
| P06 | Карточка/импорт есть; private files находятся в отдельной CRM-ветке | Две организации с одинаковым телефоном, роли и HTTP Storage isolation; upload/logout/cleanup |
| P07 | v83 — inactivity + consent + cooldown и ручной WhatsApp, не универсальные сегменты/автодоставка | Фиксированные 8 клиентов на границах отбора; точное множество; sent не считается доставкой |
| P08 | Организации/локации/роли существуют; это не разрешение общего доступа между организациями | 2 организации × 2 филиала × 3 роли, отзыв роли во время ожидания и смена контекста |
| P09 | Старый UI называет sent доставленным; исправление уже принадлежит CRM-ветке и не дублируется | Lost ACK → unknown без слепого повтора; cutover старого/нового диспетчера без дублей |
| P10 | v87 имеет attempts/refunds и зеркало v47; paid не доказывает остаток долга | Sandbox capture + два частичных возврата; зеркало не удваивает деньги; границы 1–99 копеек |
| P11 | Маржа визита не равна кассе/чистой прибыли; общий денежный журнал пока контракт | 10000 + 2000 − 1500 = 10500; перевод между кассами не меняет их общую сумму |
| P12 | v72 paid отмечает весь период, а не частичный перевод; авансы пока контракт | Начисление 10000, аванс 2000 и выплата 3000: долг 5000, расход 5000; зачёт не создаёт денег |
| P13 | Движения одного склада есть, атомарное перемещение пока контракт | Две стороны либо обе записаны, либо ни одной; mixed FIFO known/NULL и конкурентные запросы |
| P14 | v113 costing изучался в интеграции, не подтверждён этим аудитом в production | Известные и неизвестные слои, точное округление; disable/новые движения/reapply без скрытого рассинхрона |
| P15 | v73 выдаёт права, не подтверждает продажу; UI timezone исправлен, сервер ещё не проверен | Последний день срока; из 10 прав использованы 3 и возвращены 2 — доступны 5; нет resurrection после release |
| P16 | Баллы связаны с outcome, не verified cash; при отмене начисления возможен отрицательный баланс | Earn 100 → spend 80 → cancel earn: −80 по нынешней модели; повтор без дельты; отдельно политика денежных возвратов |
| P17 | Отчёты/экспорт есть, это не универсальный BI или cash-отчётность | Фиксированный набор визитов/возвратов/импорта: экран = CSV = XLSX по ID, фильтрам и суммам |
| P18 | Offline-write queue существует, но это не доказательство превосходства | Реальная Android/iOS PWA: смена пользователя, очередь ≠ подтверждённая запись, reconnect conflict/lost reply |

Доказательства первого прохода: QA запустил 9 локальных проверок CRM/организаций/
retention/уведомлений/offline/импорта; все прошли. Финансовый проход использовал
чтение кода и тестов, не новые финансовые runtime-запуски. Наличие SQL-конкурентного
теста не засчитывается за его запуск. Retention-кандидат ранее прошёл VM15/browser4,
но последующий adversarial review выявил malformed envelope: нужна новая проверка
перед выпуском. Нельзя считать этот первый проход повторной приёмкой всех функций.

## Найденное вне исходных 18 строк

| ID | Происхождение и граница | Повторная приёмка |
| --- | --- | --- |
| E-P09-VK | В узкой истории найдено прямое пожелание пользователя автоматического VK после сравнения; текущий client-messaging.js копирует текст и открывает vk.com/im вручную | Не выдавать ручной переход за бота/доставку; автоматизацию проверять отдельно на согласованном поставщике |
| E-P06-IMPORT | README прямо заявляет форматы YCLIENTS/DIKIDI/Masters; это совместимость форматов, не доказанное копирование UX | Обезличенные настоящие выгрузки, BOM/кавычки/многострочность, нормализация телефона, повтор партии, consent unknown, отмена preview пишет 0 |
| E-P09-NAMING | История подтверждает ориентацию на DIKIDI для названий и цены канала, не на YCLIENTS | Недоступный gateway не показывать подключённым; стоимость и фактический способ отправки понятны |
| E-P18-ACCESSIBILITY | Сравнительные пожелания про темы/масштаб/Android найдены, точный источник решения не подтверждён | 360/390/768 px, 200% текст, светлая/тёмная тема, фокус/клавиатура, полные контакты после обновления |

Предел полноты: код/документы рабочих веток, тематическая история Git и два узких
исторических диалога QA; весь архив всех задач не просмотрен. Для E-P09-VK QA
указал исходную сессию `01a05e91-fea8-7a90-bbb3-53d8e273199b`; цитата пользователя
доказывает пожелание, но не возможности неназванного стороннего поставщика.
Остальные заимствования продолжают инвентаризироваться. Исправления выполняются
отдельными ограниченными задачами после подтверждения причины и владельца файла.
